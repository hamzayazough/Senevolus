export enum AppRoute {
    SIGNIN = "/signin",
    HOME = "/home",
    CREATEPROFILE = "/createProfile",
    PROFILE = "/profile",
    SHOP = "/shop",
    CREATETASK = "/create-task",
    MANAGETASK = "/manage-task"
    
}