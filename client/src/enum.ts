export enum TaskStatus {
    PENDING = 'pending',
    ACTIVE = 'active',
    ACCEPTED = 'accepted'
}

export enum TaskType {
    SOCIAL = 'social',
    PHYSICAL = 'physical'
}

export enum UserType {
    ELDER = 'elder',
    VOLUNTEER = 'volunteer'
}